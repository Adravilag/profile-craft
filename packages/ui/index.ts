// Exportaciones de componentes UI
import LexicalEditorNew from './LexicalEditorNew';
import MediaLibrary from './MediaLibrary';
import AdminModal from './AdminModal';
import ImageUploadField from './ImageUploadField';

// Exportamos el componente LexicalEditorNew con el nombre LexicalEditor para mantener compatibilidad
export { 
  LexicalEditorNew as LexicalEditor, 
  MediaLibrary,
  AdminModal,
  ImageUploadField,
};
